import * as React from 'react';
import { HasComponent, HasRootRef } from '../../types';
export interface HasCaps {
    caps?: boolean;
}
export interface TypographyProps extends React.AllHTMLAttributes<HTMLElement>, HasComponent, HasRootRef<HTMLElement> {
    /**
     * Задаёт начертание шрифта, отличное от стандартного.
     */
    weight?: '1' | '2' | '3';
    /**
     * Убирает внешние отступы
     */
    normalize?: boolean;
}
export declare const Typography: ({ className, weight, Component, normalize, getRootRef, ...restProps }: TypographyProps) => React.JSX.Element;
